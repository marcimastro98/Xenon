{
  "name": "@xenon/native",
  "version": "4.11.3",
  "private": true,
  "description": "Xenon native kiosk app (Tauri) for any screen you give it, a CORSAIR Xeneon Edge included. Renders the same dashboard as the browser and the iCUE iframe, loaded from the local backend service.",
  "license": "SEE LICENSE IN ../../LICENSE",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac": "tauri build --target universal-apple-darwin",
    "build:linux": "NO_STRIP=true tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
