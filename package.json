{
  "name": "xenon-edge-widget",
  "version": "3.2.11",
  "description": "A self-contained local web widget for Corsair iCUE / Xeneon Edge dashboards: media controls, audio devices, microphone mute, calendar, notes and live system stats — all on Windows.",
  "private": true,
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "node --test \"server/test/*.test.mjs\"",
    "dev": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"try{$p=(Get-NetTCPConnection -LocalPort 3030 -State Listen -EA SilentlyContinue).OwningProcess;if($p){Stop-Process -Id $p -Force -EA SilentlyContinue;Start-Sleep -Milliseconds 600}}catch{} \" && node server/server.js",
    "icue:validate": "icuewidget validate widget",
    "icue:package": "node tools/package-icue-widget.mjs"
  },
  "engines": {
    "node": ">=18.15.0"
  },
  "os": [
    "win32"
  ],
  "license": "SEE LICENSE IN LICENSE",
  "repository": {
    "type": "git",
    "url": "https://github.com/marcimastro98/Xenon.git"
  },
  "keywords": [
    "corsair",
    "icue",
    "xeneon-edge",
    "widget",
    "windows",
    "dashboard",
    "system-monitor",
    "spotify",
    "microphone"
  ],
  "dependencies": {
    "koffi": "^3.0.2",
    "msedge-tts": "^2.0.5",
    "ws": "^8.21.0"
  }
}
